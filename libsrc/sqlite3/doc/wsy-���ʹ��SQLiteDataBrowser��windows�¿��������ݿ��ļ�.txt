1. File->Open DataBase,选中想看的alarm.db文件

2. 点"Browse Data"页，即显示类似excel格式的报警日志。按时间顺序排列；其中:

   timestr一列表示发生时间，形如<2008-02-03 14:18:14>
   type一列表示事件类型，分为以下4种；

	<ALARM>表示发生报警，此时state一列中的值表示报警触发状态（按位）
				
	<CHANGE>表示输入端子状态改变（不一定报警），state一列中的值表示端子输入状态（按位）
			 
	<ALARMACK>表示收到网关发来的报警确认，state一列中的值表示确认的结果（0为正确，其他值为错误码）
	
	<CHANGEACK>表示受到网关发来的端子输入状态改变的确认，state一列中的值表示确认结果（0为正确，其他值为错误码）

	
   最后的time和triginid两列目前暂时只为程序处理方便，不提供信息量
